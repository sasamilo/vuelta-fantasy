(() => {
  const cfg = window.FANTASY_CONFIG || {};
  const route = window.VUELTA_ROUTE || {};
  const participantImages={sasa:'https://thinktank.preskok.si/wp-content/uploads/2026/02/Sasa-Milo-768x768.webp',lovro:'https://i.imgur.com/7OdZQyk.jpg',robert:'https://preskok.si/wp-content/uploads/2025/08/Robert-Golob.webp',samo:'https://autobrief.io/wp-content/uploads/2025/09/Samo_Pavlovic-portfolio.webp',matej:'https://preskok.si/wp-content/uploads/2025/08/Matej-Klinc.webp',blaz:'https://preskok.si/wp-content/uploads/2026/05/Blaz-Lipar.webp'};
  const $=s=>document.querySelector(s), esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
  const first=n=>String(n||'').trim().split(/\s+/)[0].replace(/[.,]/g,'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();
  const participant=n=>{const x=esc(n),i=participantImages[first(n)];return i?`<span class="participant-cell"><img class="participant-avatar" src="${i}" alt="${x}" loading="lazy"> <span>${x}</span></span>`:x};
  const empty=(s,c,t)=>{const e=$(s);if(e)e.innerHTML=`<tr><td colspan="${c}" class="empty">${esc(t)}</td></tr>`};
  if(cfg.googleFormUrl){const e=$('[data-form-link]');if(e){e.href=cfg.googleFormUrl;e.hidden=false}}
  if(!cfg.supabaseUrl||!cfg.supabaseAnonKey){empty('[data-leaderboard]',4,'Configure Supabase to publish standings.');return}
  const headers={apikey:cfg.supabaseAnonKey,Authorization:`Bearer ${cfg.supabaseAnonKey}`};
  const get=async p=>{const r=await fetch(`${cfg.supabaseUrl}/rest/v1/${p}`,{headers});if(!r.ok)throw Error(`${r.status}: ${await r.text()}`);return r.json()};
  const initials=n=>{const p=String(n||'').trim().split(/\s+/).filter(Boolean);return p.length?`${p[0][0]}${p.length>1?p[p.length-1][0]:''}`.toUpperCase():'?'};
  const fallback=n=>`data:image/svg+xml;charset=UTF-8,${encodeURIComponent(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 300 400"><rect width="300" height="400" fill="#d9ff43"/><text x="150" y="220" text-anchor="middle" font-family="Arial,sans-serif" font-size="88" font-weight="900" fill="#11231f">${esc(initials(n))}</text></svg>`)}`;

  async function leaderboard(){try{const rows=await get('leaderboard?select=*&order=total_points.desc,participant_name.asc');if(!rows.length)empty('[data-leaderboard]',4,'No scored predictions yet.');else $('[data-leaderboard]').innerHTML=rows.map((x,i)=>`<tr><td class="rank">${i+1}</td><td>${participant(x.participant_name)}</td><td class="number"><strong>${x.total_points}</strong></td><td class="number">${x.stages_scored}</td></tr>`).join('')}catch(e){console.error(e);empty('[data-leaderboard]',4,'Scores are temporarily unavailable.')}}

  async function current(){
    let stages;try{stages=await get('public_stages?select=id,stage_number,stage_name,result_date,pcs_url&order=stage_number.desc&limit=1')}catch(e){console.error('Stages:',e);const x=$('[data-stage-results]');if(x)x.innerHTML='<p class="empty">Official results are temporarily unavailable.</p>';return}
    if(!stages.length)return;const stage=stages[0],n=Number(stage.stage_number)||0;
    const c=$('[data-stage-count]');if(c)c.textContent=`${n} stage${n===1?'':'s'} scored`;
    const results=await get(`public_rider_results?select=stage_id,stage_number,stage_name,finish_position,rider_name,rider_url,rider_image,points&stage_id=eq.${encodeURIComponent(stage.id)}&order=finish_position.asc`).catch(e=>{console.error('Current results:',e);return[]});
    const all=await get('public_rider_results?select=stage_id,stage_number,stage_name,finish_position,rider_name,rider_url,rider_image,points&order=stage_number.desc,finish_position.asc').catch(e=>{console.error('All results:',e);return[]});
    const container=$('[data-stage-results]');if(container){const groups=new Map();all.forEach(r=>{if(!groups.has(r.stage_id))groups.set(r.stage_id,[]);groups.get(r.stage_id).push(r)});container.innerHTML=[...groups.values()].sort((a,b)=>Number(b[0].stage_number)-Number(a[0].stage_number)).map((rs,i)=>{const s=rs[0];return `<details class="stage-result"${i===0?' open':''}><summary><span>Stage ${s.stage_number}${s.stage_name?` · ${esc(s.stage_name)}`:''}</span><span class="pill">Top 30</span></summary><div class="table-wrap"><table><thead><tr><th>Place</th><th>Rider</th><th class="number">Points</th></tr></thead><tbody>${rs.sort((a,b)=>Number(a.finish_position)-Number(b.finish_position)).map(r=>`<tr><td class="rank">${r.finish_position}</td><td>${esc(r.rider_name)}</td><td class="number"><strong>${r.points}</strong></td></tr>`).join('')}</tbody></table></div></details>`}).join('')||'<p class="empty">No official stage results are available yet.</p>'}
    const rsWinner=results.find(r=>Number(r.finish_position)===1), routeStage=route.stages?.[n];
    if(routeStage){const a=$('[data-latest-eyebrow]'),b=$('[data-latest-title]'),d=$('[data-latest-meta]'),l=$('[data-latest-link]');if(a)a.textContent=`Stage ${n}`;if(b)b.textContent=`${routeStage.start} → ${routeStage.finish}`;if(d)d.textContent=`${routeStage.type} · ${routeStage.distance} · ${routeStage.date}`;if(l){l.href=routeStage.official_url||stage.pcs_url||'#';l.hidden=false}}
    else{const b=$('[data-latest-title]'),d=$('[data-latest-meta]');if(b)b.textContent=`Stage ${n} · ${stage.stage_name||'Official result'}`;if(d)d.textContent=`${stage.result_date||'Results published'} · top 30 scored`}
    if(rsWinner){const card=$('[data-stage-winner]'),img=$('[data-winner-image]');if(card&&img){const fb=fallback(rsWinner.rider_name);img.onerror=()=>{img.onerror=null;img.src=fb};img.src=rsWinner.rider_image||fb;img.alt=rsWinner.rider_name;const name=$('[data-winner-name]'),team=$('[data-winner-team]');if(name)name.textContent=routeStage?.winner?.display_name||rsWinner.rider_name;if(team)team.textContent=routeStage?.winner?.team||'';card.href=rsWinner.rider_url||'#';card.target='_blank';card.rel='noopener';card.hidden=false}}
    const label=$('[data-picks-stage]');if(label)label.textContent=`Stage ${n}`;
    const scores=await get(`stage_scores?select=*&stage_id=eq.${encodeURIComponent(stage.id)}&order=points.desc,participant_name.asc`).catch(e=>{console.error('Daily scores:',e);return[]});
    const picks=await get(`public_predictions?select=participant_name,rider_names,stage_id&stage_id=eq.${encodeURIComponent(stage.id)}&order=participant_name.asc`).catch(e=>{console.error('Predictions:',e);return[]});
    const daily=$('[data-daily]');if(daily)daily.innerHTML=scores.length?scores.map((x,i)=>`<tr><td class="rank">${i+1}</td><td>${participant(x.participant_name)}</td><td>${(x.scoring_riders||[]).map(r=>`${esc(r.rider_name)} <small>(+${r.points})</small>`).join(', ')||'—'}</td><td class="number"><strong>${x.points}</strong></td></tr>`).join(''):'<tr><td colspan="4" class="empty">No daily scores were recorded for this stage.</td></tr>';
    const byName=new Map(scores.map(x=>[x.participant_name,x.points])),pred=$('[data-predictions]');if(pred)pred.innerHTML=picks.length?picks.map(x=>`<article class="pick-card"><h3><span class="participant-heading">${participant(x.participant_name)}</span> <span class="score-tag">${byName.get(x.participant_name)??0} pts</span></h3><ol>${(x.rider_names||[]).map(r=>`<li>${esc(r)}</li>`).join('')}</ol></article>`).join(''):'<p class="empty">No predictions recorded for this stage.</p>';
  }
  leaderboard();current();
})();
